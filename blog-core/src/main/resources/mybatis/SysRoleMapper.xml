<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.ralap.blog.persistent.mapper.SysRoleMapper">
  <resultMap id="BaseResultMap" type="com.ralap.blog.persistent.beans.SysRole">
    <id column="id" property="id" jdbcType="INTEGER"/>
    <result column="description" property="description" jdbcType="VARCHAR"/>
    <result column="available" property="available" jdbcType="BIT"/>
    <result column="create_time" property="createTime" jdbcType="TIMESTAMP"/>
    <result column="update_time" property="updateTime" jdbcType="TIMESTAMP"/>
  </resultMap>
  <sql id="Base_Column_List">
    id, description, available, create_time, update_time
  </sql>
  <select id="getCurrAndAboveRole" parameterType="int"
    resultType="com.ralap.blog.persistent.beans.SysRole">
    SELECT *
    FROM sys_role
    WHERE level <![CDATA[ >= ]]> #{level}
  </select>

  <select id="getCurrAndUnderRole" parameterType="int"
    resultType="com.ralap.blog.persistent.beans.SysRole">
    SELECT *
    FROM sys_role
    WHERE level <![CDATA[ <= ]]> #{level}
  </select>
</mapper>